spring:
  application:
    name: embabel

  datasource:
    url: jdbc:mysql://localhost:3306/embabel_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: wnsgk677400
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080